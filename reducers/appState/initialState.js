const initialState = {
  appStart: false,
  loading: true,
  systemMessage: {
    message: null,
    error: null,
  },
  deviceLocation: null,
  temporaryImage: null,
};

export default initialState;
